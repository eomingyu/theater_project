<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.idev.mapper.SchedulesMapper">
	<insert id="insertSchedule" parameterType="Schedules">
		INSERT INTO schedules(schedule_idx, perform_idx, theater, perform_date, start_time)
		VALUES(schedule_idx_seq.nextval, #{perform_idx}, #{theater}, #{perform_date}, #{start_time})
	</insert>

</mapper>