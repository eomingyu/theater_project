<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.idev.mapper.MembersMapper">
	<select id="list" resultType="Members">		<!-- 메인글에 대한 댓글 목록 가져오기 -->
	select * from members
	</select>
	<select id="getPageList" parameterType="Map" resultType="Members">
		SELECT * FROM (
			SELECT rownum AS R, T.* FROM 
				(SELECT * FROM members ORDER BY reg_date DESC) T )
		WHERE R BETWEEN #{startNo} AND #{endNo}
	</select>
	<select id="getCount" resultType="int">
		SELECT count(*) FROM members
	</select>
	
	<select id="search" parameterType="Map" resultType="Members">
		select * from members
		<if test="columns=='id'">
 where id like '%'||#{find}||'%'
		</if>
		<if test="columns=='name'">
 where name like '%'||#{find}||'%'
		</if>
		<if test="columns=='authority'">
 where authority=#{find}
		</if>
	</select>
	<select id="searchPageList" parameterType="Map" resultType="Members">
		SELECT * FROM (
			SELECT rownum AS R, T.* FROM 
				(select * from members
		<if test="columns=='id'">
 where id like '%'||#{find}||'%'
		</if>
		<if test="columns=='name'">
 where name like '%'||#{find}||'%'
		</if>
		<if test="columns=='authority'">
 where authority=#{find}
		</if>
		ORDER BY reg_date DESC) T )
		WHERE R BETWEEN #{startNo} AND #{endNo}
	</select>
	<select id="getSearchCount" parameterType="Map" resultType="int">
		SELECT count(*) FROM members
		<if test="columns=='id'">
			where id like '%'||#{find}||'%'
		</if>
		<if test="columns=='name'">
			where name like '%'||#{find}||'%'
		</if>
		<if test="columns=='authority'">
			where authority=#{find}
		</if>
	</select>
	
	
</mapper>