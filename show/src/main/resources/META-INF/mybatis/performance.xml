<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.idev.mapper.PerformanceMapper">
	<insert id="insertPerform" parameterType="Performance">
		INSERT INTO performance(perform_idx, perform_title, poster,
		 		showtime, grade, detail, start_date, end_date)
		VALUES(perform_idx_seq.nextval, #{perform_title}, #{poster},
		 		#{showtime}, #{grade}, #{detail}, #{start_date}, #{end_date})
	</insert>
	
	<select id="selectScheduledPerform" resultType="Performance">
	<![CDATA[
		SELECT * FROM performance
		WHERE start_date > sysdate+1
		ORDER BY start_date 
	]]>
	</select>
	<select id="getOne" resultType="Performance" parameterType="int">
		SELECT * FROM performance
		WHERE perform_idx=#{perform_idx}
	</select>	
</mapper>


